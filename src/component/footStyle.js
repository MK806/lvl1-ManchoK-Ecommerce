import { makeStyles } from '@material-ui/core';

const FootStyle = makeStyles((theme) => ({
      title: {
        flexGrow: 1,
      },
      botRoot: {
        backgroundColor: "grey",
      },
      fcont: {
        marginTop: "80px",
      },
      title1: {
        justifyContent: "center",
        alignText: "center",
      },
}));

export default FootStyle;