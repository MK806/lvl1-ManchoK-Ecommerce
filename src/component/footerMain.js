import { makeStyles } from '@material-ui/core';

const FootMStyle = makeStyles((theme) => ({
      Fmain: {
        backgroundColor: "#e6e6e6",
        paddingTop: "20px",
      },
}));

export default FootMStyle;